import React from 'react';
import { Box } from '@mui/material';
import RegulationManager from '../../components/regulations/RegulationManager';

const RegulationManagerPage: React.FC = () => {
    return (
        <Box>
            <RegulationManager />
        </Box>
    );
};

export default RegulationManagerPage;
